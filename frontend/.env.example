# Optional — only needed if thermal print URL differs from default localhost:9999
# Hybrid deploy: keep default; each cashier PC runs start-print-server.bat locally.
# VITE_THERMAL_PRINT_SERVER=http://localhost:9999
